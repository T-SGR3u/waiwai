.container.col-4.text-center.bg-light.mt-4
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .form-title
      パスワードをお忘れですか?
    .field.mt-4
      = f.label :email,class:"label"
      %br/
      = f.email_field :email, autofocus: true, autocomplete: "email",placeholder:"ご登録パスワードをご入力ください",class:"form-control col-12"
    .send_email_title.mt-4
      ※ご登録メールアドレスにパスワード再設定用のご案内を送信ます。
    .actions
      = f.submit "Send",class:"btn btn-success col-12"
  = render "devise/shared/links"
